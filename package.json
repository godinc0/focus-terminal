{
  "name": "focus-terminal",
  "version": "2.0.0",
  "description": "Terminal-based website blocking system",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist/*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "@crxjs/vite-plugin": "^2.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  }
}